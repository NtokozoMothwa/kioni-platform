{
  "name": "kioni",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:brain": "pnpm --filter brain dev",
    "dev:api": "pnpm --filter api dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}

